@extends('layouts.app')
@section('title', $title)
@section('content')
<div class="page-header d-print-none"><div class="container-xl"><div class="row g-2 align-items-center"><div class="col"><div class="page-pretitle">{{ $pretitle }}</div><h2 class="page-title">{{ $title }}</h2></div><div class="col-auto ms-auto"><a href="{{ route($routePrefix.'.create') }}" class="btn btn-primary">Add Record</a></div></div></div></div>
<div class="page-body"><div class="container-xl"><div class="card"><div class="card-header"><form class="row g-2 w-100" method="GET"><div class="col-md-6"><input class="form-control" type="search" name="search" value="{{ $search }}" placeholder="Search code or name"></div><div class="col-auto"><button class="btn btn-outline-primary">Search</button></div>@if($search!=='')<div class="col-auto"><a class="btn btn-outline-secondary" href="{{ route($routePrefix.'.index') }}">Clear</a></div>@endif</form></div><div class="table-responsive"><table class="table table-vcenter card-table"><thead><tr><th>Code</th><th>Name</th><th>Status</th><th class="w-1"></th></tr></thead><tbody>@forelse($records as $record)<tr><td><code>{{ $record->subject_code }}</code></td><td>{{ $record->subject_name }}</td><td><span class="badge {{ $record->is_active ? 'bg-success-lt' : 'bg-secondary-lt' }}">{{ $record->is_active ? 'Active' : 'Inactive' }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route($routePrefix.'.edit',$record) }}">Edit</a></td></tr>@empty<tr><td colspan="4" class="text-center text-secondary py-4">No records found.</td></tr>@endforelse</tbody></table></div>@if($records->hasPages())<div class="card-footer">{{ $records->links() }}</div>@endif</div></div></div>
@endsection
