@extends('layouts.app') @section('title',$definition['title']) @section('content')<div class="page-header"><div class="container-xl"><div class="row"><div class="col"><div class="page-pretitle">Central Master Data</div><h2 class="page-title">{{ $definition['title'] }}</h2></div><div class="col-auto"><a class="btn btn-primary" href="{{ route('registration-masters.create',$type) }}">Add Record</a></div></div></div></div><div class="page-body"><div class="container-xl"><div class="card"><div class="card-header"><form><input class="form-control" name="search" value="{{ $search }}" placeholder="Search code or name"></form></div><table class="table"><thead><tr>@foreach($definition['fields'] as $f)<th>{{ str($f)->headline() }}</th>@endforeach<th>Status</th><th></th></tr></thead><tbody>@foreach($records as $r)<tr>@foreach($definition['fields'] as $f)<td>{{ $r->$f }}</td>@endforeach<td>{{ $r->is_active?'Active':'Inactive' }}</td><td><a href="{{ route('registration-masters.edit',[$type,$r]) }}">Edit</a></td></tr>@endforeach</tbody></table><div class="card-footer">{{ $records->links() }}</div></div></div></div>@endsection