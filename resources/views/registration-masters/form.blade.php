@extends('layouts.app') @section('title',$definition['title']) @section('content')<div class="page-header"><div class="container-xl"><h2 class="page-title">{{ $record?'Edit':'Add' }} {{ $definition['title'] }}</h2></div></div><div class="page-body"><div class="container-xl"><form class="card" method="post" action="{{ $record?route('registration-masters.update',[$type,$record]):route('registration-masters.store',$type) }}"><div class="card-body">@csrf @if($record) @method('PUT') @endif @foreach($definition['fields'] as $f)<div class="mb-3"><label class="form-label">{{ str($f)->headline() }}</label><input class="form-control" name="{{ $f }}" value="{{ old($f,$record?->$f) }}" {{ in_array($f,['code','division_code'])?'type=number':'' }} {{ $f==='name_bn'?'':'required' }}></div>@endforeach<label class="form-check"><input type="hidden" name="is_active" value="0"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active',$record?->is_active??true))><span class="form-check-label">Active</span></label></div><div class="card-footer text-end"><button class="btn btn-primary">Save</button></div></form></div></div>@endsection