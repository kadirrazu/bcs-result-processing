@extends('layouts.app')
@section('title', 'Viva Review Listing')
@section('content')
<div class="page-header d-print-none"><div class="container-xl"><div class="row g-2 align-items-center"><div class="col"><h2 class="page-title">Viva Review Listing</h2><div class="text-secondary">Warnings are review information only. ACTIVE candidates remain in the pipeline.</div></div><div class="col-auto ms-auto"><a href="{{ route('viva.index') }}" class="btn btn-outline-secondary">Back to Viva</a></div></div></div></div>
<div class="page-body"><div class="container-xl">
<div class="card mb-3"><div class="card-body"><form method="get" class="row g-2 align-items-end"><div class="col-md-3"><label class="form-label">Review type</label><select name="review" class="form-select"><option value="all" @selected($review==='all')>All candidates</option><option value="quota" @selected($review==='quota')>Quota mismatch</option><option value="source" @selected($review==='source')>Source invalid / issue</option><option value="high" @selected($review==='high')>High-mark review</option></select></div><div class="col-md-2"><label class="form-label">Quota</label><select name="quota" class="form-select"><option value="">All</option>@foreach(['CFF','EM','PHC'] as $q)<option value="{{ $q }}" @selected($quota===$q)>{{ $q }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Mismatch direction</label><select name="direction" class="form-select"><option value="">Both</option><option value="registration_only" @selected($direction==='registration_only')>Registration only</option><option value="viva_only" @selected($direction==='viva_only')>Viva Board only</option></select></div><div class="col-md-3"><label class="form-label">Search</label><input name="search" class="form-control" value="{{ $search }}" placeholder="REG, User, Code or name"></div><div class="col-auto"><button class="btn btn-primary">Filter</button></div><div class="col-auto"><a href="{{ route('viva.reviews') }}" class="btn btn-outline-secondary">Clear</a></div></form></div></div>
<div class="card"><div class="card-header"><div><h3 class="card-title">Review records</h3><div class="text-secondary small">Displaying {{ number_format($rows->firstItem() ?? 0) }} to {{ number_format($rows->lastItem() ?? 0) }} of {{ number_format($rows->total()) }} records</div></div></div><div class="table-responsive"><table class="table table-vcenter card-table"><thead><tr><th>REG / USER</th><th>CODE</th><th>NAME</th><th>TRACK</th><th>MARK</th><th>REG. QUOTA</th><th>VIVA QUOTA</th><th>REVIEW REASON</th><th>STATUS</th><th></th></tr></thead><tbody>
@forelse($rows as $row)
@php
$details = is_array($row->quota_mismatch_details) ? $row->quota_mismatch_details : (json_decode((string)$row->quota_mismatch_details, true) ?: []);
$reasons=[];
foreach($details as $type=>$detail){$reasons[]=$type.' '.(($detail['direction']??'')==='registration_only'?'present in Registration only':'marked by Viva Board only');}
if($row->invalid_flag)$reasons[]='Source invalid flag';
if($row->issue_flag)$reasons[]='Source issue flag';
if($row->high_mark_review)$reasons[]='High-mark review';
$regQuota=[]; if((int)$row->has_ff_quota>0)$regQuota[]='CFF'; if((int)$row->has_em_quota>0)$regQuota[]='EM'; if((int)$row->has_phc_quota>0)$regQuota[]='PHC';
$vivaQuota=[]; if($row->viva_cff)$vivaQuota[]='CFF'; if($row->viva_em)$vivaQuota[]='EM'; if($row->viva_phc)$vivaQuota[]='PHC';
@endphp
<tr class="{{ $reasons ? 'table-warning' : '' }}"><td><div class="fw-semibold">{{ $row->reg }}</div><div class="text-secondary small">{{ $row->user_id }}</div></td><td>{{ $row->code }}</td><td>{{ $row->name ?? '—' }}</td><td>{{ $row->written_qualified_track instanceof \BackedEnum ? $row->written_qualified_track->value : $row->written_qualified_track }}</td><td>{{ $row->attendance_status === 'absent' ? 'ABS' : number_format((float)$row->mark,2) }}</td><td>{{ $regQuota ? implode(', ',$regQuota) : 'None' }}</td><td>{{ $vivaQuota ? implode(', ',$vivaQuota) : 'None' }}</td><td>@if($reasons)@foreach($reasons as $reason)<div>{{ $reason }}</div>@endforeach @else<span class="text-secondary">No current review flag</span>@endif</td><td>@php($statusValue = $row->status instanceof \BackedEnum ? $row->status->value : (string) $row->status)<span class="badge {{ $statusValue === 'active' ? 'bg-green-lt text-green' : 'bg-secondary-lt text-secondary' }}">{{ strtoupper($statusValue) }}</span></td><td class="text-end"><a href="{{ route('viva.candidates.edit', $row->id) }}" class="btn btn-sm btn-outline-primary">View / Edit</a></td></tr>
@empty<tr><td colspan="10" class="text-center text-secondary py-4">No records match the selected review filters.</td></tr>@endforelse
</tbody></table></div>@if($rows->hasPages())<div class="card-footer">{{ $rows->links() }}</div>@endif</div>
</div></div>
@endsection
