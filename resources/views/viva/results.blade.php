@extends('layouts.app')
@section('title', 'Viva Results')
@section('content')
<div class="page-header d-print-none"><div class="container-xl"><div class="row g-2 align-items-center"><div class="col"><h2 class="page-title">Confidential Viva Results</h2><div class="text-secondary">Internal administrative view. Viva marks and PASS/FAIL are not public publishing data.</div></div><div class="col-auto ms-auto"><a href="{{ route('viva.index') }}" class="btn btn-outline-secondary">Back to Viva</a></div></div></div></div>
<div class="page-body"><div class="container-xl">
<div class="alert alert-warning"><strong>CONFIDENTIAL:</strong> This page and its exports are for authorized internal administrative use only.</div>
<div class="card mb-3"><div class="card-body"><form method="get" class="row g-2 align-items-end">
<div class="col-md-3"><label class="form-label">Search</label><input name="search" class="form-control" value="{{ $search }}" placeholder="REG, User, Code or Name"></div>
<div class="col-md-2"><label class="form-label">Result</label><select name="result" class="form-select"><option value="">All</option>@foreach(['pass'=>'PASS','fail'=>'FAIL','not_applicable'=>'NOT APPLICABLE','pending'=>'PENDING'] as $v=>$l)<option value="{{ $v }}" @selected($resultStatus===$v)>{{ $l }}</option>@endforeach</select></div>
<div class="col-md-2"><label class="form-label">Attendance</label><select name="attendance" class="form-select"><option value="">All</option><option value="appeared" @selected($attendance==='appeared')>Appeared</option><option value="absent" @selected($attendance==='absent')>Absent</option></select></div>
<div class="col-md-2"><label class="form-label">Status</label><select name="status" class="form-select"><option value="">All</option>@foreach(['active'=>'ACTIVE','cancelled'=>'CANCELLED','withheld'=>'WITHHELD','expelled'=>'EXPELLED'] as $v=>$l)<option value="{{ $v }}" @selected($candidateStatus===$v)>{{ $l }}</option>@endforeach</select></div>
<div class="col-md-1"><label class="form-label">Review</label><select name="review" class="form-select"><option value="">All</option><option value="warning" @selected($review==='warning')>Warning</option></select></div>
<div class="col-auto"><button class="btn btn-primary">Filter</button></div><div class="col-auto"><a href="{{ route('viva.results.index') }}" class="btn btn-outline-secondary">Clear</a></div>
</form></div></div>
<div class="card mb-3"><div class="card-header"><div><h3 class="card-title">Internal Result Exports</h3><div class="text-secondary small">Authorized XLSX only — no TXT or publishing DOCX.</div></div></div><div class="card-body d-flex gap-2 flex-wrap">@foreach(['all'=>'All','pass'=>'PASS','fail'=>'FAIL','absent'=>'ABS','cancelled'=>'Cancelled','withheld'=>'Withheld','expelled'=>'Expelled','warning'=>'Warnings'] as $scope=>$label)<a href="{{ route('viva.results.export',['scope'=>$scope]) }}" class="btn btn-sm btn-outline-primary">{{ $label }} XLSX</a>@endforeach</div></div>
<div class="card"><div class="card-header"><div><h3 class="card-title">Processed Viva Records</h3><div class="text-secondary small">Displaying {{ number_format($rows->firstItem() ?? 0) }} to {{ number_format($rows->lastItem() ?? 0) }} of {{ number_format($rows->total()) }} records</div></div></div><div class="table-responsive"><table class="table table-vcenter card-table"><thead><tr><th>REG / USER</th><th>CODE</th><th>NAME</th><th>TRACK</th><th>MARK</th><th>ATTENDANCE</th><th>STATUS</th><th>RESULT</th><th>FAIL REASON</th><th>VERSION</th><th></th></tr></thead><tbody>
@forelse($rows as $row)
@php($statusValue=$row->status instanceof \BackedEnum?$row->status->value:(string)$row->status)
@php($resultValue=$row->viva_result_status instanceof \BackedEnum?$row->viva_result_status->value:(string)$row->viva_result_status)
<tr><td><div class="fw-semibold">{{ $row->reg }}</div><div class="text-secondary small">{{ $row->user_id }}</div></td><td>{{ $row->code }}</td><td>{{ $row->name ?? '—' }}</td><td>{{ $row->written_qualified_track }}</td><td class="fw-semibold">{{ $row->attendance_status==='absent'?'ABS':number_format((float)$row->mark,2) }}</td><td>{{ strtoupper($row->attendance_status) }}</td><td>{{ strtoupper($statusValue) }}</td><td><span class="badge {{ $resultValue==='pass'?'bg-green-lt text-green':($resultValue==='fail'?'bg-red-lt text-red':'bg-secondary-lt text-secondary') }}">{{ strtoupper(str_replace('_',' ',$resultValue)) }}</span></td><td>{{ is_array($row->viva_fail_reasons)?implode(', ',$row->viva_fail_reasons):'—' }}</td><td>{{ $row->processing_version ?: '—' }}</td><td class="text-end"><a href="{{ route('viva.candidates.edit',$row->id) }}" class="btn btn-sm btn-outline-primary">Details</a></td></tr>
@empty<tr><td colspan="11" class="text-center text-secondary py-4">No processed Viva records match the selected filters.</td></tr>@endforelse
</tbody></table></div>@if($rows->hasPages())<div class="card-footer">{{ $rows->links() }}</div>@endif</div>
</div></div>
@endsection
