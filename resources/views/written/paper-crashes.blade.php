@extends('layouts.app')
@section('title','Written Paper Crash Report')
@section('content')
<div class="page-header"><div class="container-xl"><div class="row align-items-center"><div class="col"><h2 class="page-title">Written Paper Crash Report</h2><div class="text-secondary">Actual marks are preserved; counted marks are zero after the configured crash rule.</div></div><div class="col-auto"><a class="btn btn-outline-secondary" href="{{ route('written.index') }}">Back to Written</a></div></div></div></div>
<div class="page-body"><div class="container-xl">
<div class="card mb-3"><div class="card-body"><form method="get" class="row g-2"><div class="col-md-3"><label class="form-label">Subject</label><select class="form-select" name="subject"><option value="all">All crashed subjects</option>@foreach($subjects as $code)<option value="{{ $code }}" @selected($filters['subject']===$code)>{{ $code }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">REG / USER</label><input class="form-control" name="search" value="{{ $filters['search'] }}"></div><div class="col-md-auto align-self-end"><button class="btn btn-primary">Filter</button></div><div class="col-md-auto align-self-end"><a class="btn btn-outline-secondary" href="{{ route('written.paper-crashes') }}">Reset</a></div></form></div></div>
<div class="card"><div class="table-responsive"><table class="table table-vcenter card-table"><thead><tr><th>REG</th><th>USER</th><th>Category</th><th>Qualified Track</th><th>Subject</th><th class="text-end">Actual</th><th class="text-end">Threshold</th><th class="text-end">Counted</th></tr></thead><tbody>@forelse($rows as $row)<tr class="table-warning"><td>{{ $row->reg }}</td><td>{{ $row->user_id }}</td><td>{{ [1=>'GG',2=>'TT',3=>'GT'][(int)$row->cadre_category]??$row->cadre_category }}</td><td>{{ $row->written_qualified_track ?: '—' }}</td><td>{{ $row->subject_code }}</td><td class="text-end">{{ number_format((float)$row->actual_mark,2) }}</td><td class="text-end">{{ number_format((float)$row->crash_threshold,2) }}</td><td class="text-end">{{ number_format((float)$row->counted_mark,2) }}</td></tr>@empty<tr><td colspan="8" class="text-center text-secondary py-4">No paper crash records.</td></tr>@endforelse</tbody></table></div>@if($rows->hasPages())<div class="card-footer">{{ $rows->links() }}</div>@endif</div>
</div></div>
@endsection
