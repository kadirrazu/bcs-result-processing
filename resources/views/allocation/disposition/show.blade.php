@extends('layouts.app')
@section('content')
<div class="page-header"><div class="container-xl"><div class="row align-items-center"><div class="col"><h2 class="page-title">A5.5 — Candidate Publication Status</h2><div class="text-secondary">Reg: {{ $registration->reg }} · {{ $registration->name }}</div></div><div class="col-auto ms-auto"><a class="btn btn-outline-secondary" href="{{ route('allocation.disposition.index') }}">Back to A5.5</a></div></div></div></div>
<div class="page-body"><div class="container-xl">
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="row row-cards mb-3"><div class="col-lg-5"><div class="card h-100"><div class="card-header"><h3 class="card-title">Current Allocation</h3></div><div class="card-body"><dl class="row mb-0"><dt class="col-5">Reg</dt><dd class="col-7">{{ $registration->reg }}</dd><dt class="col-5">Name</dt><dd class="col-7">{{ $registration->name }}</dd><dt class="col-5">Allocated Cadre</dt><dd class="col-7"><strong>{{ $allocation->cadre_code }} - {{ $abbr }}</strong></dd><dt class="col-5">Allocation Basis</dt><dd class="col-7">{{ strtoupper((string)$allocation->allocation_basis) }}</dd><dt class="col-5">Publication Status</dt><dd class="col-7"><span class="badge bg-{{ $effectiveStatus==='ACTIVE'?'success':($effectiveStatus==='WITHHELD'?'warning':'danger') }}-lt">{{ $effectiveStatus }}</span></dd></dl></div></div></div>
<div class="col-lg-7"><div class="card h-100"><div class="card-header"><h3 class="card-title">Change Publication Status</h3></div><div class="card-body"><div class="alert alert-warning py-2"><strong>No seat release / no reallocation.</strong> This action changes publication only. The finalized A4/A5 allocation remains immutable.</div><form method="POST" action="{{ route('allocation.disposition.update',$registration->id) }}">@csrf @method('PUT')<div class="mb-3"><label class="form-label">New Status</label><select class="form-select" name="status" required><option value="ACTIVE">ACTIVE</option><option value="WITHHELD">WITHHELD</option><option value="CANCELLED">CANCELLED</option></select></div><div class="mb-3"><label class="form-label">Reason <span class="text-danger">*</span></label><textarea class="form-control" rows="4" name="reason" required>{{ old('reason') }}</textarea></div><div class="mb-3"><label class="form-label">Reference No. / Memo (optional)</label><input class="form-control" name="reference_no" value="{{ old('reference_no') }}"></div><button class="btn btn-primary">Update Status</button></form></div></div></div></div>
<div class="card"><div class="card-header"><h3 class="card-title">Disposition Audit History</h3></div><div class="table-responsive"><table class="table table-vcenter mb-0"><thead><tr><th>Time</th><th>From</th><th>To</th><th>Reason</th><th>Reference</th><th>Operator</th></tr></thead><tbody>@forelse($audits as $audit)<tr><td>{{ $audit->created_at?->format('d-m-Y h:i A') }}</td><td>{{ $audit->from_status }}</td><td><strong>{{ $audit->to_status }}</strong></td><td class="text-wrap">{{ $audit->reason }}</td><td>{{ $audit->reference_no ?: '—' }}</td><td>@if($audit->actor_id){{ $audit->actor_id }} - {{ $operators->get($audit->actor_id,'Unknown') }}@else System @endif</td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-4">No manual disposition changes. Candidate is ACTIVE by default.</td></tr>@endforelse</tbody></table></div></div>
</div></div>
@endsection
