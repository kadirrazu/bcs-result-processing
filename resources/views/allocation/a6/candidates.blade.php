@extends('layouts.app')
@section('content')
<div class="page-header"><div class="container-xl"><div class="row align-items-center"><div class="col"><h2 class="page-title">A6 — Reporting &amp; Export - Candidate Search</h2><div class="text-secondary">Current finalized Tabulation-eligible population. Open a candidate for consolidated module-wise detail.</div></div><div class="col-auto ms-auto"><a class="btn btn-outline-secondary" href="{{ route('allocation.a6.index') }}">A6 — Reporting &amp; Export</a></div></div></div></div>
<div class="page-body"><div class="container-xl">
<div class="row row-cards mb-3">
    <div class="col-sm-6 col-lg-3"><div class="card"><div class="card-body"><div class="text-secondary">Total Candidates</div><div class="h1 mb-0">{{ number_format($totalCandidates) }}</div><div class="small text-secondary">Viva Passed / Tabulation-eligible</div></div></div></div>
    @if($search !== '')<div class="col-sm-6 col-lg-3"><div class="card"><div class="card-body"><div class="text-secondary">Search Result</div><div class="h1 mb-0">{{ number_format($results->total()) }}</div><div class="small text-secondary">Matching candidates</div></div></div></div>@endif
</div>
<div class="card"><div class="card-body"><form class="row g-2" method="GET"><div class="col-md-6"><input class="form-control" name="search" value="{{ $search }}" placeholder="Search Reg / User ID / Name"></div><div class="col-auto"><button class="btn btn-primary">Search</button></div><div class="col-auto"><a class="btn btn-outline-secondary" href="{{ route('allocation.a6.candidates') }}">Reset</a></div></form></div><div class="table-responsive"><table class="table table-vcenter mb-0"><thead><tr><th class="text-center">SL</th><th>Reg</th><th>User</th><th>Name</th><th>Track</th><th>Allocation</th><th></th></tr></thead><tbody>@forelse($results as $row)<tr><td class="text-center">{{ $results->firstItem()+$loop->index }}</td><td><strong>{{ $row->reg }}</strong></td><td>{{ $row->registration_user_id }}</td><td>{{ $row->candidate_name }}</td><td>{{ strtoupper((string)($row->written_qualified_track ?? '—')) }}</td><td>@if($a=$allocatedRegs->get($row->reg))<span class="badge bg-success-lt">{{ $a->cadre_code }} - {{ $allocationAbbr->get((int)$a->cadre_code,'—') }} - {{ strtoupper((string)$a->allocation_basis) }}</span>@else<span class="badge bg-secondary-lt">NOT ALLOCATED</span>@endif</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('allocation.a6.candidate',$row->reg) }}">Details</a></td></tr>@empty<tr><td colspan="7" class="text-center text-secondary py-4">No candidates found.</td></tr>@endforelse</tbody></table></div>@if($results->hasPages())<div class="card-footer">{{ $results->links() }}</div>@endif</div></div></div>
@endsection
