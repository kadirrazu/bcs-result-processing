@extends('layouts.app')
@section('title','NC3 — Non-Cadre Choice Validation & Adjustment')
@section('page-header')<div class="row align-items-center"><div class="col"><div class="page-pretitle">Non Cadre Processing</div><h2 class="page-title">NC3 — Choice Validation & Adjustment</h2></div></div>@endsection
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger"><strong>Upload blocked.</strong><ul class="mb-0">@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif
<div class="row row-cards mb-3"><div class="col-md-4"><div class="card h-100"><div class="card-body"><div class="subheader">Configured Maximum Choices</div><div class="h1 mb-0">{{ $maxChoices }}</div><div class="text-secondary">Dynamic columns: opt01 … opt{{ str_pad((string)$maxChoices,2,'0',STR_PAD_LEFT) }}</div></div></div></div><div class="col-md-8"><div class="card h-100"><div class="card-body"><div class="subheader">Candidate Authority</div><div class="fw-semibold">Written/Viva qualified Cadre-allocation population</div><div class="text-secondary mt-1">Cadre-allocated candidates are retained as Historical Only. Empty choice rows are preserved. NC4 uses only eligible, non-Cadre-allocated candidates with an effective choice.</div></div></div></div></div>
<div class="card mb-3"><div class="card-header"><h3 class="card-title">Upload Choice Excel</h3><div class="card-actions"><a class="btn btn-outline-primary" href="{{ route('non-cadre.choice.template') }}">Download Sample Template</a></div></div><div class="card-body"><p class="text-secondary">Exact headers: <code>{{ implode(', ', $headers) }}</code>. Import runs in the queue; this page does not wait for spreadsheet validation.</p><form method="post" action="{{ route('non-cadre.choice.upload') }}" enctype="multipart/form-data" class="row g-2 align-items-end">@csrf<div class="col-md-9"><label class="form-label">Excel file</label><input type="file" name="file" class="form-control" required accept=".xlsx,.xls"></div><div class="col-md-3"><button class="btn btn-primary w-100">Upload & Queue Validation</button></div></form></div></div>
<div class="card"><div class="card-header"><h3 class="card-title">Choice Dataset Versions</h3></div><div class="table-responsive"><table class="table table-vcenter"><thead><tr><th>Version</th><th>Status</th><th class="text-center">Progress</th><th class="text-center">Rows</th><th class="text-center">Eligible</th><th class="text-center">Historical</th><th class="text-center">Empty</th><th class="text-center">Invalid</th><th></th></tr></thead><tbody>@forelse($imports as $i) @php($s=$summaries[$i->id]??[])<tr><td class="fw-bold">v{{ $i->version }}</td><td><span class="badge bg-{{ $i->status==='finalized'?'green':(in_array($i->status,['queued','processing'])?'azure':($i->status==='failed'?'red':'secondary')) }}-lt">{{ strtoupper(str_replace('_',' ',$i->status)) }}</span></td><td class="text-center">{{ (int)($i->progress_percent??0) }}%</td><td class="text-center">{{ $s['total']??$i->total_rows }}</td><td class="text-center text-success fw-bold">{{ $s['allocation_eligible']??0 }}</td><td class="text-center text-blue">{{ $s['historical_only']??0 }}</td><td class="text-center text-secondary">{{ $s['empty']??0 }}</td><td class="text-center text-danger">{{ $s['invalid']??0 }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('non-cadre.choice.version',$i->id) }}">Open</a></td></tr>@empty<tr><td colspan="9" class="text-center text-secondary py-4">No Non-Cadre Choice dataset uploaded yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection
