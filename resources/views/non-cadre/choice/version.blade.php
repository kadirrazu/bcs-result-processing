@extends('layouts.app')
@section('title','NC3 Choice v'.$record->version)
@section('page-header')<div class="row align-items-center"><div class="col"><div class="page-pretitle">NC3 — Choice Validation & Adjustment</div><h2 class="page-title">Choice Dataset v{{ $record->version }}</h2></div><div class="col-auto"><a href="{{ route('non-cadre.choice.index') }}" class="btn btn-outline-secondary">Back</a></div></div>@endsection
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif
@if(in_array($record->status,['queued','processing']))
<div class="card mb-3" id="nc-choice-progress" data-url="{{ route('non-cadre.choice.progress',$record->id) }}"><div class="card-body"><div class="d-flex justify-content-between"><strong>Queue Processing</strong><span id="nc-choice-status">{{ strtoupper($record->status) }}</span></div><div class="progress mt-2"><div id="nc-choice-bar" class="progress-bar" style="width:{{ (int)($record->progress_percent??0) }}%"></div></div><div class="small text-secondary mt-2" id="nc-choice-count">{{ (int)($record->processed_rows??0) }} / {{ (int)$record->total_rows }} rows · {{ (int)($record->progress_percent??0) }}%</div><div class="small text-secondary mt-1">You may leave this page; validation continues in the queue.</div></div></div>
@endif
@if($record->status==='failed')<div class="alert alert-danger"><strong>Import failed.</strong> {{ $record->failure_message }}</div>@endif
<div class="row row-cards mb-3">@foreach(['total'=>'Total Rows','allocation_eligible'=>'Allocation Eligible','historical_only'=>'Cadre Allocated — Historical','empty'=>'Empty Choice','no_valid_choice'=>'No Valid Choice','invalid'=>'Invalid','adjusted_candidates'=>'Manually Adjusted','excluded_choices'=>'Excluded Choices'] as $k=>$label)<div class="col-6 col-md-3"><div class="card h-100"><div class="card-body"><div class="subheader">{{ $label }}</div><div class="h2 mb-0">{{ number_format($summary[$k]??0) }}</div></div></div></div>@endforeach</div>
<div class="card mb-3"><div class="card-body d-flex justify-content-between align-items-center"><div><strong>Status:</strong> {{ strtoupper(str_replace('_',' ',$record->status)) }} · <strong>Max Choices:</strong> {{ $record->max_choices }} · <strong>Valid:</strong> {{ $record->valid_rows }} · <strong>Invalid:</strong> {{ $record->invalid_rows }}</div>@if($record->status==='validated' && (int)$record->invalid_rows===0)<form method="post" action="{{ route('non-cadre.choice.finalize',$record->id) }}">@csrf<button class="btn btn-success">Finalize NC3 Dataset</button></form>@endif</div></div>
<div class="card mb-3"><div class="card-body"><form method="get" class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label">Search</label><input class="form-control" name="search" value="{{ $search }}" placeholder="Registration or User ID"></div><div class="col-md-4"><label class="form-label">Candidate / Choice Status</label><select class="form-select" name="filter"><option value="all" @selected($filter==='all')>All Candidates</option><option value="eligible" @selected($filter==='eligible')>Non-Cadre Allocation Eligible</option><option value="adjusted" @selected($filter==='adjusted')>Manually Adjusted</option><option value="historical" @selected($filter==='historical')>Cadre Allocated — Historical Only</option><option value="not_eligible" @selected($filter==='not_eligible')>No Valid Choice / Not Allocation Eligible</option><option value="empty" @selected($filter==='empty')>Empty Choice</option><option value="invalid" @selected($filter==='invalid')>Invalid Candidate / Identity</option></select></div><div class="col-auto"><button class="btn btn-primary">Filter</button></div><div class="col-auto"><a class="btn btn-outline-secondary" href="{{ route('non-cadre.choice.version',$record->id) }}">Reset</a></div></form></div></div>
<div class="card"><div class="table-responsive"><table class="table table-vcenter mb-0"><thead><tr><th style="width:170px">Candidate / Choice</th><th>Information</th></tr></thead><tbody>
@forelse($items as $item)
@php($sum=json_decode($item->validation_summary,true)?:[]) @php($orig=array_values(array_filter(json_decode($item->original_choices,true)?:[],fn($v)=>trim((string)$v)!==''))) @php($val=json_decode($item->validated_choices,true)?:[]) @php($eff=json_decode($item->effective_choices,true)?:[])
<tr class="bg-light"><td colspan="2"><div class="d-flex align-items-center justify-content-between gap-3"><div class="d-flex flex-wrap align-items-center gap-3"><div><span class="text-secondary">Reg</span> <strong>{{ $item->reg }}</strong></div><div><span class="text-secondary">User</span> <strong>{{ $item->user_id }}</strong></div><span class="badge bg-{{ ($sum['population_status']??'')==='ALLOCATION_ELIGIBLE'?'green':(($sum['population_status']??'')==='INVALID'?'red':'blue') }}-lt">{{ str_replace('_',' ',($sum['population_status']??'UNKNOWN')) }}</span>@if(isset($sum['common_merit_position']))<span class="small">Common Merit: <strong class="fw-bold">{{ $sum['common_merit_position'] }}</strong></span>@endif</div><a class="btn btn-sm btn-outline-primary" href="{{ route('non-cadre.choice.item.show',$item->id) }}">Details</a></div></td></tr>
<tr><td colspan="2" class="px-3 py-2"><div class="fw-bold mb-2">Original Choice</div>@include('non-cadre.choice.partials.choice-lane',['codes'=>$orig,'badgeClass'=>'bg-secondary-lt','emptyText'=>'Empty Choice List'])</td></tr>
<tr><td colspan="2" class="px-3 py-2"><div class="fw-bold mb-2">Validated Choice</div>@include('non-cadre.choice.partials.choice-lane',['codes'=>$val,'badgeClass'=>'bg-blue-lt','emptyText'=>'No Valid Choice'])</td></tr>
<tr class="border-bottom border-2"><td colspan="2" class="px-3 py-2"><div class="fw-bold mb-2">Allocation Ready Choice</div>@include('non-cadre.choice.partials.choice-lane',['codes'=>$eff,'badgeClass'=>'bg-green-lt','emptyText'=>'No Allocation Ready Choice'])</td></tr>
@empty<tr><td colspan="2" class="text-center text-secondary py-4">No candidates match the selected filter.</td></tr>@endforelse
</tbody></table></div>@if($items->hasPages())<div class="card-footer">{{ $items->links() }}</div>@endif</div>
@if(in_array($record->status,['queued','processing']))
<script>document.addEventListener('DOMContentLoaded',()=>{const box=document.getElementById('nc-choice-progress');if(!box)return;const poll=()=>fetch(box.dataset.url,{headers:{'Accept':'application/json'}}).then(r=>r.json()).then(d=>{document.getElementById('nc-choice-status').textContent=String(d.status).toUpperCase().replaceAll('_',' ');document.getElementById('nc-choice-bar').style.width=d.progress_percent+'%';document.getElementById('nc-choice-count').textContent=d.processed_rows+' / '+d.total_rows+' rows · '+d.progress_percent+'%';if(['queued','processing'].includes(d.status))setTimeout(poll,1500);else location.reload();}).catch(()=>setTimeout(poll,2500));setTimeout(poll,1000);});</script>
@endif
@endsection
