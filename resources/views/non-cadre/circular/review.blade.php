@extends('layouts.app')
@section('title', 'Review Non-Cadre Circular Import')
@section('page-header')<div class="row g-2 align-items-center"><div class="col"><div class="page-pretitle">NC1 — Non-Cadre Circular</div><h2 class="page-title">Import Validation Review</h2></div><div class="col-auto"><a href="{{ route('non-cadre.circular.index') }}" class="btn btn-outline-secondary">Back</a></div></div>@endsection
@section('content')
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="row row-cards mb-3"><div class="col-md-3"><div class="card"><div class="card-body"><div class="subheader">Rows</div><div class="h2">{{ $batch->total_rows }}</div></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><div class="subheader">Valid</div><div class="h2 text-success">{{ $batch->valid_rows }}</div></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><div class="subheader">Invalid</div><div class="h2 text-danger">{{ $batch->invalid_rows }}</div></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><div class="subheader">Status</div><div class="h3">{{ strtoupper(str_replace('_',' ',$batch->status)) }}</div></div></div></div></div>
<div class="card mb-3"><div class="table-responsive"><table class="table table-vcenter card-table"><thead><tr><th>Row</th><th>Post Code</th><th>Post</th><th>Count</th><th>Status</th><th>Validation</th></tr></thead><tbody>@foreach($rows as $row)@php($d=json_decode($row->normalized_data ?? '{}',true))@php($e=json_decode($row->validation_errors ?? '[]',true))<tr><td>{{ $row->row_number }}</td><td class="fw-bold">{{ $d['post_code'] ?? '—' }}</td><td>{{ $d['post_title'] ?? '—' }}</td><td>{{ $d['post_count'] ?? '—' }}</td><td>{{ $d['status'] ?? '—' }}</td><td>@if($row->validation_status==='valid')<span class="badge bg-green-lt">VALID</span>@else<span class="badge bg-red-lt">INVALID</span><div class="small text-danger mt-1">{{ implode(' ', $e ?: []) }}</div>@endif</td></tr>@endforeach</tbody></table></div></div>{{ $rows->links() }}
@if((int)$batch->invalid_rows===0 && $batch->status==='validated')<form method="post" action="{{ route('non-cadre.circular.import.approve',$batch->id) }}">@csrf<button class="btn btn-primary">Approve as New Draft Version</button></form>@else<div class="alert alert-warning">Approval is blocked until every row is valid. Correct the Excel file and upload it again; invalid data is never silently merged.</div>@endif
@endsection
