# Choice Optimization CO4B.4 — Contract Test Alignment

Tests-only maintenance patch.

- Fixes PHP interpolation in source-contract assertions for `$cadre`, `$status`, and `$invalid`.
- Updates the older CO4A UI wording contract to the current `Repository Authority` + `Approve & Make Effective` workflow.
- No application code, schema, repository behavior, validation logic, approval logic, navigation, or UI behavior changed.
