# Choice Optimization CO4C2.1 — Historical Visual Confirmation Polish

- Historical source match table now labels comparison columns with current and previous BCS numbers.
- Current and Previous candidate identity blocks use the same order: Registration, Name, Father.
- A centered serial number column is shown before Current Candidate and remains pagination-aware.
- Individual Historical Match Review resolves the current district code through the central District Master and shows `code - title`.
- Operator resolution audit resolves the central User name at runtime and shows `Confirmed by: User #ID (Name)` / equivalent rejection wording.
- District title and user name are runtime reference data and are not redundantly copied into historical match rows.
