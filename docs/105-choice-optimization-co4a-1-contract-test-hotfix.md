# Choice Optimization CO4A.1 — Contract Test Hotfix

- Fixes PHP variable interpolation in the CO4A source-contract test.
- `$bcsNumber` is now asserted as a literal source-code token.
- No application code, schema, import behavior, or business rule changed.
